// ЯЗЫКИ ГЛАВНОЙ ВХОДА — одно место (шаг 300, 2026-09-25). Их читают страница `app/[lang]` (какие адреса предрендерить) и
// `proxy.ts` (куда вести корень и незнакомый язык). Прокси не может импортировать саму страницу — в него уехал бы React,
// поэтому список живёт здесь. Добавил язык — добавь его и в слова страницы (`WORDS` в `app/[lang]/page.tsx`).
export const PRESENTATION_LANGS = ["en", "ru"] as const
export const DEFAULT_PRESENTATION_LANG = "en"
